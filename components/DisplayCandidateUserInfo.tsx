'use client'

export default function DisplayCandidateInfo({ profileInfo }: { profileInfo: string }) {
    const parsedProfileInfo = JSON.parse(profileInfo);
    const {  } = parsedProfileInfo;

    return (
        <div>
            Table
        </div>
    )
}